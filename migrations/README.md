Alembic migration folder for the Ian Racing Intelligence Platform.

The MVP currently uses the existing SQLite models. Future migrations should be
generated here as the database layer moves toward PostgreSQL-ready deployment.
